// this file is not working; not sure why
export default function useCloseAlert(isAlertOpen) {
  if (isAlertOpen = true) {
    return isAlertOpen = false
  } else if (isAlertOpen.value = true) {
    return isAlertOpen.value = false
  }
}